node_modules/
dist/

# Real secrets — never commit
.env
.mcp.json

# Local Claude Code overrides — user-specific, never commit
.claude/settings.local.json

# Docs
docs/

# Pipeline run output
data/runs/
data/checkpoints/*.json

# Cached run snapshots — contain real lead data, never commit
cached-run/

# Local tool caches
.deepline/
.firecrawl/
.playwright-mcp/
.vscode/

# macOS
.DS_Store

.trigger